loads a csv into the specified kdb process

- types of the csv are interpreted by the file
- in the config, you can specify multiple directories or paths where to load data into a specific table

usage:
./ksv -c path/to/config
